name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check-marketplace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check the marketplace manifest and the plugins agree
        run: script/check-marketplace.sh
